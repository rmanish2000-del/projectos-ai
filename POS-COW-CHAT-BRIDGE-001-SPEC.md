# POS-COW-CHAT-BRIDGE-001 — Handoff Package Specification

Status: **SPEC ONLY — not executed.** Cowork has no filesystem access to `C:\ProjectOS-AI`.
Owner for execution: **Claude Code** (repository operations are Claude Code's lane per routing policy).

This document is the complete, deterministic build instruction for the handoff package.
Claude Code executes it verbatim against the real repository. No architecture decisions are
made here; every value marked `<...>` must be filled from real repository evidence.

---

## 0. Pre-flight (run first, paste output into the files below)

```bash
cd /c/ProjectOS-AI            # PowerShell: cd C:\ProjectOS-AI
git rev-parse --abbrev-ref HEAD
git rev-parse HEAD
git status --porcelain
git log -1 --format='%H%n%an%n%aI%n%s'
```

Working tree MUST be clean before the package is generated. If dirty, stop and report —
do not stash, do not commit unrelated work.

File hashes (canonical method — SHA-256, forward-slash paths, sorted):

```bash
git ls-files -z | sort -z | xargs -0 sha256sum > /tmp/hashes.txt
```

Only files that are **tracked** are hashed. Untracked files are listed separately as gaps.

Create the folder:

```bash
mkdir -p projectos-handoff
```

---

## 1. `projectos-handoff/CURRENT_STATE.md`

```markdown
# ProjectOS AI — Current State

Generated: <ISO-8601 UTC timestamp>
Generated by: Claude Code, assignment POS-COW-CHAT-BRIDGE-001
Repository: C:\ProjectOS-AI
Branch: <branch>
Commit SHA: <full 40-char SHA>
Commit date: <ISO-8601>
Working tree: clean

## Purpose of this package
Snapshot of ProjectOS AI repository state for review in Claude Chat.
Claude Chat has no repository access; this package is the sole evidence base
for the review requested in CHAT_REVIEW_REQUEST.md.

## Repository shape
<tree of tracked files, depth 3, generated from `git ls-files`>

## What exists today
| Area | Artefact | Status | Evidence (path) |
|---|---|---|---|
| <area> | <file> | canonical / proposed / blocked / superseded | <path> |

## What does not exist yet
- <explicit list of expected-but-absent artefacts>

## Last verified activity
- Last commit: <SHA short> — <subject> (<author>, <date>)
- CI status: <verified / not configured / unknown — state which>
- Tests: <verified pass / none present / not run — state which>

## Evidence classification
Every claim above is one of: Verified (read from repo/git), Reported, Assumed, Blocked.
Claims in this file are **Verified** unless explicitly tagged otherwise inline.
```

Rule: do not describe intent, roadmap, or design opinion in this file. State only what
the repository contains.

---

## 2. `projectos-handoff/CANONICAL_MANIFEST.yaml`

```yaml
manifest_version: 1
assignment_id: POS-COW-CHAT-BRIDGE-001
generated_utc: "<ISO-8601>"
repository:
  path: "C:\\ProjectOS-AI"
  branch: "<branch>"
  commit_sha: "<40-char SHA>"
  working_tree_clean: true
hash_algorithm: sha256

# Status vocabulary is closed. Do not invent new values.
#   canonical   - authoritative, frozen or in force
#   proposed    - drafted, not yet accepted
#   blocked     - cannot progress, see OPEN_BLOCKERS.md
#   superseded  - replaced; must name successor
documents:
  - path: "<repo-relative path, forward slashes>"
    status: canonical
    sha256: "<hash>"
    bytes: <int>
    last_commit: "<SHA>"
    supersedes: null
    superseded_by: null
    notes: "<one line, factual>"

  - path: "<path>"
    status: superseded
    sha256: "<hash>"
    bytes: <int>
    last_commit: "<SHA>"
    supersedes: null
    superseded_by: "<path of successor>"   # REQUIRED when status: superseded
    notes: ""

untracked_files:
  - "<path>"        # present on disk, not in git — treated as non-evidence

# MANDATORY DISCLOSURE. Gitignored files do not appear as untracked, so an
# omission here is invisible rather than merely absent. Any ignored path whose
# content is governance, policy, architecture, or decision material MUST be
# listed. Generated caches, build output, and machine-local settings need not be.
# If this list is empty, state that it was checked and found empty.
ignored_content_paths:
  - path: "<path>"
    ignored_by: "<.gitignore rule that excludes it>"
    content_type: governance / policy / architecture / decision / other
    reason_excluded: "<one line>"
    reviewable: true / false   # can Claude Chat be shown this out-of-band?

counts:
  canonical: <int>
  proposed: <int>
  blocked: <int>
  superseded: <int>
  untracked: <int>
  ignored_content: <int>
```

Known at time of writing (verify, do not assume):
- `.projectos/policies.yaml.txt` — governance policy content excluded by the
  `.projectos/` rule. Must appear in `ignored_content_paths`.
- `Projects/**` — project registrations excluded by the `Projects/` rule.
  Disclose if any contain decisions rather than generated state.

Validation rule: every `superseded_by` and `supersedes` value MUST appear as a `path`
in this same manifest, or the package fails acceptance ("all references resolve").

---

## 3. `projectos-handoff/DECISION_REGISTER.md`

```markdown
# Founder Decision Register — Active

Generated: <ISO-8601>
Source of truth: repository documents listed under Evidence. Conversation memory is NOT evidence.

| ID | Decision | Date | Status | Evidence (repo path + SHA) | Affects |
|---|---|---|---|---|---|
| FD-001 | <verbatim or tightly paraphrased decision> | <date> | Active / Superseded | <path> @ <commit SHA> | <modules> |

## Superseded decisions
| ID | Decision | Superseded by | Date |
|---|---|---|---|

## Decisions asserted in conversation but NOT found in the repository
Listed as **Reported (unverified)**. These are gaps, not decisions.
- <item> — no repository evidence found.
```

Rule: do not create, reinterpret, or resolve decisions. Transcribe and cite only.

---

## 4. `projectos-handoff/OPEN_BLOCKERS.md`

```markdown
# Open Blockers

Generated: <ISO-8601>

| ID | Blocker | Blocks | Type | Owner | Since | Evidence | Needed to clear |
|---|---|---|---|---|---|---|---|
| BLK-001 | <statement> | <assignment/module> | founder-decision / access / architecture / external / security | Founder / Claude Code / Claude Chat | <date> | <path or "none — reported"> | <the single specific input required> |

## Not blockers
Items that are merely incomplete work, with an owner and a path forward, are excluded.
```

Type vocabulary is closed. Owner must be exactly one party.

---

## 5. `projectos-handoff/CHAT_REVIEW_REQUEST.md`

```markdown
# Claude Chat Review Request

Package commit SHA: <SHA>
Branch: <branch>
Assignment: POS-COW-CHAT-BRIDGE-001

## What Claude Chat must review
Exactly these questions. Do not answer questions not on this list.

1. <question — must be a high-risk-trigger question: architecture conflict, research
   methodology, risk calculation, legal/regulatory, security boundary, breaking contract,
   frozen-module change, or ambiguous ownership>
2. <...>

## What Claude Chat must NOT do
- Do not propose implementation.
- Do not restate the roadmap.
- Do not review documents outside CANONICAL_MANIFEST.yaml.
- Do not treat `proposed` documents as decided.

## Required response format
For each numbered question, return exactly one of:
- **ACCEPT** — with the accepted position stated in one sentence.
- **REJECT** — with the blocking reason in one sentence.
- **NEEDS FOUNDER DECISION** — with the decision framed as a binary or enumerated choice.

Write the result into LAST_CHAT_DECISION.md and return it. No essays.

## Inputs Claude Chat is given
See OUTPUT_INDEX.md for upload order.
```

---

## 6. `projectos-handoff/OUTPUT_INDEX.md`

```markdown
# Handoff Package Index

Commit SHA: <SHA>   Branch: <branch>   Generated: <ISO-8601>

## Upload order into Claude Chat
1. CURRENT_STATE.md          — orientation; read first
2. CANONICAL_MANIFEST.yaml   — what is authoritative
3. DECISION_REGISTER.md      — constraints already fixed
4. OPEN_BLOCKERS.md          — what is stuck
5. CHAT_REVIEW_REQUEST.md    — the ask; read last, answer only this
6. LAST_CHAT_DECISION.md     — empty placeholder; Chat writes into it

Referenced canonical documents (upload only if a review question cites them):
7. <path> — <why>

## File inventory
| File | sha256 | bytes |
|---|---|---|
| CURRENT_STATE.md | <hash> | <n> |
| CANONICAL_MANIFEST.yaml | <hash> | <n> |
| DECISION_REGISTER.md | <hash> | <n> |
| OPEN_BLOCKERS.md | <hash> | <n> |
| CHAT_REVIEW_REQUEST.md | <hash> | <n> |
| LAST_CHAT_DECISION.md | <hash> | <n> |

(OUTPUT_INDEX.md itself is not self-hashed.)
```

---

## 7. `projectos-handoff/LAST_CHAT_DECISION.md`

Empty governed placeholder — created with this exact content and nothing else:

```markdown
# Last Claude Chat Decision

STATUS: EMPTY — no Claude Chat decision has been recorded.

This file is written ONLY by a Claude Chat review response against a stated package
commit SHA. Claude Code must not populate it. Cowork must not populate it.

<!-- Schema for the next writer:

Package commit SHA: <SHA>
Decision date: <ISO-8601>
Reviewer: Claude Chat

| Q# | Verdict | Statement |
|----|---------|-----------|
| 1  | ACCEPT / REJECT / NEEDS FOUNDER DECISION | <one sentence> |

Resulting actions:
- <action> — owner: <one owner>

-->
```

---

## 8. Commit (separate, package-only)

```bash
git add projectos-handoff/
git status --porcelain          # must show ONLY projectos-handoff/ additions
git commit -m "chore(handoff): add POS-COW-CHAT-BRIDGE-001 Claude Chat handoff package

Snapshot of repository state at <parent SHA> for Claude Chat review.
No canonical documents modified."
git status --porcelain          # must be empty
git rev-parse HEAD              # report this SHA
```

Note: the package's own commit SHA differs from the SHA recorded *inside* the files
(which is the parent commit — the state being described). Record both in the report.
This is expected and must be stated explicitly, not hidden.

---

## 9. Acceptance checklist

- [ ] `projectos-handoff/` contains exactly 7 files.
- [ ] Every `<...>` placeholder replaced with real data.
- [ ] Commit SHA is real and matches `git rev-parse`.
- [ ] Every hash produced by sha256 over the real file bytes.
- [ ] Every path referenced in any file resolves to a real file.
- [ ] Every `superseded_by` / `supersedes` target exists in the manifest.
- [ ] No canonical document modified (`git diff <parent> --stat` shows only additions under `projectos-handoff/`).
- [ ] Working tree clean after commit.
- [ ] Package not self-reviewed; report states this.
- [ ] `ignored_content_paths` populated, or explicitly stated as checked-and-empty.

## 10. Required report format

```
Files:        <absolute paths, 7>
Package SHA:  <SHA of handoff commit>
State SHA:    <parent SHA described by the package>
Purpose:      <2 lines>
Gaps:         <unresolved items, or "none">
Upload order: <1-6 per OUTPUT_INDEX.md>
```
