# Copy to /etc/projectos/engine.env (mode 0644, root:projectos).
# Sourced by systemd EnvironmentFile= and by wake-linux.sh.
# Allowed: grok | codex | claude
# Default remains codex until you change this file and reload units.
ENGINE=codex
