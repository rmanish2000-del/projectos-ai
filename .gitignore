__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
*.tmp

# --- Workspace runtime-state (machine-local / generated; not corpus, POS-CC-CORPUS-COMMIT-003) ---
# Generated by `workspace init` / `add-project`; carries machine-specific absolute paths
# and per-machine registrations. The kernel's own `.projectos/` state lives here too.
/.projectos/
/Workspace.yaml
/Projects/
# Root-level pack sync artifacts (.md.txt duplicates of Shared/Packs content).
/packs/
