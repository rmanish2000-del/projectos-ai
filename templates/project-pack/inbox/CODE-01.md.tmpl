CODE-01 · SEAT: CODE · project __NAME__
Issued by PROJECTOS (scaffolder), __DATE__.

# Repo skeleton verification + first build gate

1. VERIFY the scaffold from the repository, not from this file's claims:
   C:\__NAME__ is a git repo on main, pushed to its private origin;
   README_FOR_AI.md, wake-prompt.md, build_order.json, .gitignore, docs/
   exist; the Drive root G:\My Drive\__NAME__ carries AGENT-REPORTS(/DOCS),
   INBOX, DONE. Report anything missing as a defect, not a shrug.
2. FIRST BUILD GATE: add the project's first CI-or-local check that fails
   on a broken tree (even if the tree is only docs today: a link/format
   check counts). A project with no red-possible gate has no green.
3. Do NOT scaffold product code. That waits for CHAT-01's answers and
   COWORK-01's backlog (fail closed: no product work without a declared
   first-rupee path).

REPORT BACK — four lines to this project's AGENT-REPORTS, Drive only.
State the SEAT-BOOT LAW-VERSION.
