# __NAME__ — routing for AI seats

__DESC__

Scaffolded __DATE__ by `projectos new` (ProjectOS project-pack templates —
fix the template, not the copy). This file routes; it is not the law.

- **The law**: `SEAT-BOOT.md` in the fleet's AGENT-REPORTS. Re-read it at
  every boot and wake; state its LAW-VERSION in every report. A rule that
  is not in that file does not bind a seat.
- **This project's Drive root**: `G:\My Drive\__NAME__` — AGENT-REPORTS
  (+/DOCS), INBOX, DONE, per FLEET-PROTOCOL-V1. Take only INBOX files
  tagged with your seat name or ALL, oldest first, one at a time, claim
  file first.
- **Build order**: `build_order.json` in this repo. Item 1 is the product
  one-pager (CHAT-01 in INBOX). **No build item beyond CODE-01's skeleton
  runs until CHAT-01 is answered** — no product work without a declared
  first-rupee path (founder ruling, BOOTSTRAP-AMENDMENT-1).
- **Wake**: `wake-prompt.md` in this repo; the scheduled-task template is
  `docs/wake-task.cmd`, NOT registered — enabling a wake is the founder's
  act.
- **Reports**: Drive only, one line in chat (the filename). Timestamps are
  read from a clock, never assumed.
